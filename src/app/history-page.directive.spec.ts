import { HistoryPageDirective } from './history-page.directive';

describe('HistoryPageDirective', () => {
  it('should create an instance', () => {
    const directive = new HistoryPageDirective();
    expect(directive).toBeTruthy();
  });
});
